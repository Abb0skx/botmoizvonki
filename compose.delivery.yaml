services:
  delivery-bot:
    build:
      context: .
      dockerfile: Dockerfile.delivery
    container_name: texnikach-delivery-bot
    restart: unless-stopped
    init: true
    env_file:
      - .env.delivery
    volumes:
      - delivery-data:/app/data

volumes:
  delivery-data:
    name: texnikach-delivery-data
