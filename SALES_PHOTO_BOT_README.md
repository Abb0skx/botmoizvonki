# Sales Photo OCR Bot

Отдельный Telegram-бот, который локально читает с фотографии только `IMEI`,
`IMEI2` и `S/N`, после чего превращает исходный пост в карточку продажи.
Справочник моделей, Gemini, GPT, Google Search и другие внешние AI/API не нужны.

## Как работает

1. Сотрудник публикует фотографию товара в заданном Telegram-канале. Исходная
   подпись, если она есть, переносится как `📞 Клиент: ...`.
2. Встроенные в Docker-контейнер Tesseract OCR и ZXing читают подписи `IMEI`,
   `IMEI2`, `S/N`, `SN` или `Serial No` и расположенные рядом штрихкоды. Фото
   никуда, кроме Telegram, не отправляется.
3. IMEI публикуется только как 15 цифр с корректной контрольной суммой Luhn.
   Невалидное или конфликтующее чтение пропускается. S/N подтверждается двумя
   отдельными этикетками либо совпадением текста с ближайшим штрихкодом — бот не
   угадывает спорные `O/0`, `I/1` и `S/5`.
4. Бот сначала публикует копию фотографии по Telegram `file_id`, записывает ID
   новой карточки и только затем удаляет оригинал. Если безопасно подтвердить
   публикацию не удалось, оригинал остаётся.
5. Кнопки `Olmas`, `Otabek`, `Ali`, `Abbos` добавляют менеджера и заменяются
   кнопкой `↩️ Назад`. Кнопка «Назад» возвращает четыре имени.

Нераспознанные поля полностью пропускаются. Пример подписи:

```text
📞 Клиент: 1234

🛒💵:
rasxod:

> IMEI: 490154203237518
> IMEI2: 352099001761481
> S/N: R8YL50R510N

Наличка
💵:
🇺🇿:

Card/Terminal/Paynet
💵:
🇺🇿:
```

В Telegram идентификаторы оформляются настоящими цитатами (`blockquote`).

## Почему используется канал

Сотрудники Telegram не могут вручную редактировать сообщение, отправленное ботом
в обычной группе. В канале администраторы могут дописать поставщика, цену,
`rasxod` и способы оплаты в опубликованную ботом карточку.

Боту нужны права администратора канала:

- публикация сообщений;
- удаление сообщений.

Если `SALES_PHOTO_ALLOWED_USER_IDS` пуст, менеджерские кнопки доступны только
администраторам канала. Можно дополнительно перечислить Telegram user ID через
запятую.

## Настройка

Реальный `.env.sales-photo` не коммитится. Обязательны только:

- `SALES_PHOTO_BOT_TOKEN` — токен отдельного бота от BotFather;
- `SALES_PHOTO_CHAT_ID` — отрицательный ID канала.

Шаблон находится в `.env.sales-photo.example`. Если токен попадал в переписку,
лог или репозиторий, его нужно перевыпустить в BotFather до production-запуска.

При первом запуске бот очищает накопившиеся Telegram updates, чтобы не обработать
старые фотографии. Флаг первого запуска хранится в SQLite. В базе остаются только
технические ID сообщений, состояние кнопок и зашифрованный временный retry-payload.
Фото и распознанные IMEI/S/N бот в SQLite не сохраняет.

## Проверка локально

На macOS сначала нужен Tesseract (`brew install tesseract`), затем:

```bash
python3 -m pip install -r requirements-sales-photo.txt
python3 -m unittest -q test_sales_photo_bot.py test_sales_photo_ocr.py
```

Запуск без Docker:

```bash
set -a
source .env.sales-photo
set +a
python3 -m sales_photo_bot.main
```

## Docker

Tesseract и английский языковой пакет устанавливаются внутрь образа; на VPS
устанавливать OCR-пакеты не нужно.

```bash
docker compose --env-file .env.sales-photo -f compose.sales-photo.yaml up -d --build
```

Нужна ровно одна polling-реплика. Volume `/app/data` должен быть постоянным,
а deployment — stop-first, чтобы два процесса с одним Telegram-токеном не работали
одновременно. После запуска следует отправить тестовую фотографию и проверить:

- правильность IMEI/S/N по самой этикетке;
- удаление оригинала только после появления карточки;
- четыре кнопки менеджеров и кнопку `Назад`;
- возможность администратора вручную заполнить финансовые поля.
